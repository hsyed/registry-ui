'use strict';

import React from 'react/addons';

var Header = React.createClass({

  render() {
    return (
      <header>

        Workiva Docker Registry

      </header>
    );
  }

});

export default Header;
